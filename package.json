{
  "name": "scenarios",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "build:data": "python3 scripts/build_data.py",
    "build:pdf": "python3 scripts/build_methodology_pdf.py",
    "build:card": "python3 scripts/build_social_card.py",
    "typecheck": "tsc --noEmit",
    "test:visual": "playwright test",
    "test:browser": "playwright test --grep-invert \"at [0-9]+px|looks right at\"",
    "test:visual:update": "rm -rf tests/screenshots && playwright test --update-snapshots",
    "test:all": "vitest run && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^22.20.1",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
